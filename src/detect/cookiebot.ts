/**
 * @deprecated Superseded by ./cmp.ts, which detects any CMP (not just
 * Cookiebot). Kept only as a re-export for backward compatibility.
 */
export { scanFileForCmp as scanFileForCookiebot, type CmpMatch } from "./cmp.js";
